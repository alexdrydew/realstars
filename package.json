{
  "name": "realstars",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Detects fake GitHub stars using signals from CMU's StarScout research",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "lint:firefox": "npx --yes web-ext lint --source-dir firefox-extension",
    "build:firefox": "npx --yes web-ext build --source-dir firefox-extension --artifacts-dir web-ext-artifacts --filename realstars-firefox.xpi --overwrite-dest"
  }
}
